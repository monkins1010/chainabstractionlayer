export declare class Address {
    address: string;
    derivationPath?: string;
    publicKey?: string;
    constructor(fields?: {
        address: string;
        derivationPath?: string;
        publicKey?: string;
    });
}
